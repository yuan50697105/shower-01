server:
  http2:
    enabled: true
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/learning?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    schema: classpath*:sql/*.sql
    initialization-mode: embedded
logging:
  level:
    web: debug