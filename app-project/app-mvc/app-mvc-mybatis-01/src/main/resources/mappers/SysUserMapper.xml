<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.yuan.boot.app.mapper.SysUserMapper">
    <sql id="Base_Column_List">
            username,
            `password`,
            real_name,
            nick_name
    </sql>
    <resultMap id="BaseResult" type="org.yuan.boot.app.pojo.SysUser">
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="nick_name" property="nickName"/>
    </resultMap>
    <select id="selectList" resultMap="BaseResult">
        select id, username, password, real_name, nick_name
        from sys_user
    </select>
    <insert id="insert">
        insert into sys_user (username, password, real_name, nick_name)
        values (#{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, #{realName,jdbcType=VARCHAR},
        #{nickName,jdbcType=VARCHAR});
    </insert>

    <!--auto generated by MybatisCodeHelper on 2019-12-16-->
    <insert id="insertSelective">
        INSERT INTO sys_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username!=null">username,</if>
            <if test="password!=null">password,</if>
            <if test="realName!=null">real_name,</if>
            <if test="nickName!=null">nick_name</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username!=null">#{username},</if>
            <if test="password!=null">#{password},</if>
            <if test="realName!=null">#{realName},</if>
            <if test="nickName!=null">#{nickName}</if>
        </trim>
    </insert>


</mapper>
